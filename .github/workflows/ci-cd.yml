name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  DOCKER_USER: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}

  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: ⬇️ Checkout repository

      - uses: actions/setup-node@v3
        name: ⎔ Setup Node.js
        with:
          node-version: 22
          cache: 'npm'

      - run: npm install
        name: 📥 Install dependencies

      - run: npm run format
        name: 💅 Prettier format

      - run: npm run lint
      - name: 🔬 Linting

      - run: npm run check
        name: Prettier check

      - run: npm run build
        name: 🏗 Build Next.js

      - name: 🐳 Docker Login
        if: env.DOCKER_USER
        run: echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login -u "${{ env.DOCKER_USER }}" --password-stdin

      - name: 📦 Docker Build & Push
        if: env.DOCKER_USER
        run: |
          IMAGE_NAME=${{ env.DOCKER_USER }}/paro-master

          echo "🧹 Зупинка та видалення старого контейнера (якщо є)"
          docker rm -f paro-master || true

          echo "🧼 Видалення старого образу (якщо є)"
          docker rmi -f $IMAGE_NAME:latest || true

          echo "🔨 Збірка нового образу"
          docker build -t $IMAGE_NAME:latest .

          echo "🏷️ Тегування комітом"
          docker tag $IMAGE_NAME:latest $IMAGE_NAME:${{ github.sha }}

          echo "🚀 Пуш у DockerHub"
          docker push $IMAGE_NAME:latest
          docker push $IMAGE_NAME:${{ github.sha }}

      - name: 🐳 Run Docker Container
        run: |
          IMAGE_NAME=${{ env.DOCKER_USER }}/paro-master
          echo "🌀 Старт нового контейнера"
          docker run -d --name paro-master \
            -p 3030:3000 \
            -e NODE_ENV=production \
            $IMAGE_NAME:latest \
            sh -c "next start -H 0.0.0.0 -p 3000"

          echo "⏳ Очікування запуску контейнера..."
          sleep 10

      - name: 📡 Healthcheck
        run: |
          echo "Checking application..."
          if curl -sf http://localhost:3030; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=failure" >> $GITHUB_OUTPUT
            exit 1

          echo "📦 Логи контейнера:"
          docker logs paro-master

      - name: Send Telegram Notification
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_CHAT_ID }}
          token: ${{ env.TELEGRAM_BOT_TOKEN }}
          message: |
            ✅ CI/CD завершен!
            📦 Образ: ${{ github.sha }}
            🩺 Статус HealthCheck: ${{ steps.healthcheck.outputs.status }}
            🔗 Репозиторий: ${{ github.repository }}
